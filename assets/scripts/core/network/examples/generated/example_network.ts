// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v7.34.1
// source: example_network.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "framework.network.example";

export interface ExampleEchoRequest {
  text: string;
  clientTimeMs: number;
}

export interface ExampleEchoResponse {
  text: string;
  serverTimeMs: number;
}

export interface ExampleNoticePush {
  text: string;
  level: number;
}

function createBaseExampleEchoRequest(): ExampleEchoRequest {
  return { text: "", clientTimeMs: 0 };
}

export const ExampleEchoRequest: MessageFns<ExampleEchoRequest> = {
  encode(message: ExampleEchoRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.text !== "") {
      writer.uint32(10).string(message.text);
    }
    if (message.clientTimeMs !== 0) {
      writer.uint32(16).uint64(message.clientTimeMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExampleEchoRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExampleEchoRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.clientTimeMs = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ExampleEchoRequest>): ExampleEchoRequest {
    return ExampleEchoRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ExampleEchoRequest>): ExampleEchoRequest {
    const message = createBaseExampleEchoRequest();
    message.text = object.text ?? "";
    message.clientTimeMs = object.clientTimeMs ?? 0;
    return message;
  },
};

function createBaseExampleEchoResponse(): ExampleEchoResponse {
  return { text: "", serverTimeMs: 0 };
}

export const ExampleEchoResponse: MessageFns<ExampleEchoResponse> = {
  encode(message: ExampleEchoResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.text !== "") {
      writer.uint32(10).string(message.text);
    }
    if (message.serverTimeMs !== 0) {
      writer.uint32(16).uint64(message.serverTimeMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExampleEchoResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExampleEchoResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.serverTimeMs = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ExampleEchoResponse>): ExampleEchoResponse {
    return ExampleEchoResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ExampleEchoResponse>): ExampleEchoResponse {
    const message = createBaseExampleEchoResponse();
    message.text = object.text ?? "";
    message.serverTimeMs = object.serverTimeMs ?? 0;
    return message;
  },
};

function createBaseExampleNoticePush(): ExampleNoticePush {
  return { text: "", level: 0 };
}

export const ExampleNoticePush: MessageFns<ExampleNoticePush> = {
  encode(message: ExampleNoticePush, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.text !== "") {
      writer.uint32(10).string(message.text);
    }
    if (message.level !== 0) {
      writer.uint32(16).uint32(message.level);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExampleNoticePush {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExampleNoticePush();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.level = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ExampleNoticePush>): ExampleNoticePush {
    return ExampleNoticePush.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ExampleNoticePush>): ExampleNoticePush {
    const message = createBaseExampleNoticePush();
    message.text = object.text ?? "";
    message.level = object.level ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
