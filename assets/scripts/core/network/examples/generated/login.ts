// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v7.34.1
// source: login.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "framework.network.login";

/** @msgid 1001 */
export interface reqLogin {
  deviceId: string;
  deviceBrand: string;
  channel: string;
  clientVersion: string;
}

/** @msgid 1002 */
export interface rspLogin {
  name: string;
  gems: number;
  coins: number;
  serverTime: number;
  pingInterval: number;
  level: number;
  icon: string;
  playTotalCount: number;
  playWinCount: number;
  sex: number;
  crateTime: number;
  result: number;
}

/** @msgid 1003 */
export interface ntfLogout {
  reason: number;
  description: string;
}

function createBasereqLogin(): reqLogin {
  return { deviceId: "", deviceBrand: "", channel: "", clientVersion: "" };
}

export const reqLogin: MessageFns<reqLogin> = {
  encode(message: reqLogin, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.deviceId !== "") {
      writer.uint32(10).string(message.deviceId);
    }
    if (message.deviceBrand !== "") {
      writer.uint32(18).string(message.deviceBrand);
    }
    if (message.channel !== "") {
      writer.uint32(26).string(message.channel);
    }
    if (message.clientVersion !== "") {
      writer.uint32(34).string(message.clientVersion);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): reqLogin {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasereqLogin();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.deviceId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.deviceBrand = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.channel = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.clientVersion = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<reqLogin>): reqLogin {
    return reqLogin.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<reqLogin>): reqLogin {
    const message = createBasereqLogin();
    message.deviceId = object.deviceId ?? "";
    message.deviceBrand = object.deviceBrand ?? "";
    message.channel = object.channel ?? "";
    message.clientVersion = object.clientVersion ?? "";
    return message;
  },
};

function createBaserspLogin(): rspLogin {
  return {
    name: "",
    gems: 0,
    coins: 0,
    serverTime: 0,
    pingInterval: 0,
    level: 0,
    icon: "",
    playTotalCount: 0,
    playWinCount: 0,
    sex: 0,
    crateTime: 0,
    result: 0,
  };
}

export const rspLogin: MessageFns<rspLogin> = {
  encode(message: rspLogin, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.gems !== 0) {
      writer.uint32(16).int32(message.gems);
    }
    if (message.coins !== 0) {
      writer.uint32(24).int64(message.coins);
    }
    if (message.serverTime !== 0) {
      writer.uint32(32).int32(message.serverTime);
    }
    if (message.pingInterval !== 0) {
      writer.uint32(40).uint32(message.pingInterval);
    }
    if (message.level !== 0) {
      writer.uint32(48).int32(message.level);
    }
    if (message.icon !== "") {
      writer.uint32(58).string(message.icon);
    }
    if (message.playTotalCount !== 0) {
      writer.uint32(64).int32(message.playTotalCount);
    }
    if (message.playWinCount !== 0) {
      writer.uint32(72).int32(message.playWinCount);
    }
    if (message.sex !== 0) {
      writer.uint32(80).int32(message.sex);
    }
    if (message.crateTime !== 0) {
      writer.uint32(88).int32(message.crateTime);
    }
    if (message.result !== 0) {
      writer.uint32(96).int32(message.result);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): rspLogin {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaserspLogin();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.gems = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.coins = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.serverTime = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.pingInterval = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.level = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.icon = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.playTotalCount = reader.int32();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.playWinCount = reader.int32();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.sex = reader.int32();
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.crateTime = reader.int32();
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.result = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<rspLogin>): rspLogin {
    return rspLogin.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<rspLogin>): rspLogin {
    const message = createBaserspLogin();
    message.name = object.name ?? "";
    message.gems = object.gems ?? 0;
    message.coins = object.coins ?? 0;
    message.serverTime = object.serverTime ?? 0;
    message.pingInterval = object.pingInterval ?? 0;
    message.level = object.level ?? 0;
    message.icon = object.icon ?? "";
    message.playTotalCount = object.playTotalCount ?? 0;
    message.playWinCount = object.playWinCount ?? 0;
    message.sex = object.sex ?? 0;
    message.crateTime = object.crateTime ?? 0;
    message.result = object.result ?? 0;
    return message;
  },
};

function createBasentfLogout(): ntfLogout {
  return { reason: 0, description: "" };
}

export const ntfLogout: MessageFns<ntfLogout> = {
  encode(message: ntfLogout, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reason !== 0) {
      writer.uint32(8).int32(message.reason);
    }
    if (message.description !== "") {
      writer.uint32(18).string(message.description);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ntfLogout {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasentfLogout();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.reason = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.description = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ntfLogout>): ntfLogout {
    return ntfLogout.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ntfLogout>): ntfLogout {
    const message = createBasentfLogout();
    message.reason = object.reason ?? 0;
    message.description = object.description ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
